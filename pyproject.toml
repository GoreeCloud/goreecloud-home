[project]
name = "goreecloud-home"
version = "0.1.0.dev5"
description = "Local-first GoreeCloud smart-home core"
requires-python = ">=3.12"
dependencies = []

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]